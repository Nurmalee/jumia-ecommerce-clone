import styled from 'styled-components'

const Cart = () => {
    return (
        <CartContainer>
            <h1>I am the cart in-view</h1>
        </CartContainer>
    )
}

export default Cart

const CartContainer = styled.div`

`
